{% extends 'base.html.twig' %}

{% block title %}{{title}}{% endblock %}

{% block body %}
<section class="vh-90">
    <a href="{{ path('app_gallery_details',{id: car.id})}}" class=" btn btn-primary mx-auto">Retour</a>
    <div class="row d-flex justify-content-center align-items-center h-90 mt-5">
        <div class="col-12 col-md-9 col-lg-7 col-xl-6">
            <div class="card" style="border-radius: 15px;">
                <div class="card-body p-5">
                    <h2 class="text-uppercase text-center mb-5">Contactez-nous</h2>
                    {{ form(form, {'method': 'POST'}) }}
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}